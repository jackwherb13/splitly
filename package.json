{
  "name": "splitly",
  "version": "0.0.0",
  "private": true,
  "description": "Shared house ledger — see SPEC.md",
  "scripts": {
    "build": "npm --prefix web run build",
    "lint": "npm --prefix web run lint && ruff check api",
    "test": "npm --prefix web run test && cd api && python -m pytest -q",
    "verify": "npm run lint && npm run build && npm run test"
  }
}
