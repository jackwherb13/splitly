# build artifacts — single location (SPEC §C37)
dist/

# deps
node_modules/
.venv/
__pycache__/
*.egg-info/
*.pyc
.pytest_cache/
.ruff_cache/

# terraform — provider cache + state (SPEC §V14).
# .terraform.lock.hcl is NOT ignored: it pins provider hashes for CI.
.terraform/
*.tfstate
*.tfstate.*

# env / secrets
.env
.env.*.local

# editor / os
.DS_Store
Thumbs.db
